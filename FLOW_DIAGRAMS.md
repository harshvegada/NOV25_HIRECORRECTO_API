# API Test Flow - Visual Diagrams

## 🎯 Scenario 1: MCQ Answer Submission Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                    START TEST EXECUTION                         │
└────────────────────────────┬────────────────────────────────────┘
                             ↓
                   ┌─────────────────────┐
                   │  @Test Method       │
                   │  testMCQSubmission  │
                   └──────────┬──────────┘
                              ↓
                   ┌─────────────────────┐
                   │  Initialize Context │
                   │  ScreeningControl:  │
                   │  - screeningId      │
                   │  - jobRoleID        │
                   │  - applicationID    │
                   └──────────┬──────────┘
                              ↓
                   ┌─────────────────────┐
                   │  Create MCQService  │
                   │  instance           │
                   └──────────┬──────────┘
                              ↓
         ┌────────────────────────────────────────┐
         │  mcqService.submitMCQAnswer(           │
         │    questionId: "Q123",                 │
         │    experience: "3.0",                  │
         │    answer: "Option A"                  │
         │  )                                     │
         └────────────────┬───────────────────────┘
                          ↓
         ┌────────────────────────────────────────┐
         │  MCQService builds payload             │
         │  ┌──────────────────────────────────┐  │
         │  │ MCQRootPayload.builder()         │  │
         │  │   .questionId("Q123")            │  │
         │  │   .type("mcq")                   │  │
         │  │   .candidateAnswer("Option A")   │  │
         │  │   .copyPasteAnalysis(            │  │
         │  │     CopyPasteAnalysis            │  │
         │  │       .createDefault(257)        │  │ ← SHARED UTILITY
         │  │   )                              │  │
         │  │   .build()                       │  │
         │  └──────────────────────────────────┘  │
         └────────────────┬───────────────────────┘
                          ↓
         ┌────────────────────────────────────────┐
         │  Convert to JSON                       │
         │  JavaToJSON.convertToJSON(payload)     │
         └────────────────┬───────────────────────┘
                          ↓
         ┌────────────────────────────────────────┐
         │  Call inherited BaseService method     │
         │  updateCandidateResult(                │
         │    candidateScreeningId,               │
         │    jsonPayload                         │
         │  )                                     │
         └────────────────┬───────────────────────┘
                          ↓
         ┌────────────────────────────────────────┐
         │  BaseService prepares request          │
         │  - setJsonHeaders()                    │
         │  - setBody(jsonPayload)                │
         └────────────────┬───────────────────────┘
                          ↓
         ┌────────────────────────────────────────┐
         │  APIControlActions.executePatchAPI()   │
         │  ┌──────────────────────────────────┐  │
         │  │ Build RestAssured request:       │  │
         │  │ - Add headers                    │  │
         │  │ - Add body                       │  │
         │  │ - Set base URI from config       │  │
         │  │ - Set endpoint path              │  │
         │  └──────────────────────────────────┘  │
         └────────────────┬───────────────────────┘
                          ↓
         ┌────────────────────────────────────────┐
         │  Execute HTTP PATCH Request            │
         │  PATCH /api/candidateScreening/        │
         │        update-candidate-result/        │
         │        {candidateScreeningId}          │
         │  Headers:                              │
         │    Content-Type: application/json      │
         │    Accept: application/json            │
         │  Body: { MCQ JSON payload }            │
         └────────────────┬───────────────────────┘
                          ↓
              ┌──────────────────────┐
              │   API SERVER         │
              │   Processes Request  │
              └──────────┬───────────┘
                         ↓
         ┌────────────────────────────────────────┐
         │  HTTP Response Received                │
         │  Status: 200 OK                        │
         │  Body: { success: true, ... }          │
         └────────────────┬───────────────────────┘
                          ↓
         ┌────────────────────────────────────────┐
         │  Response returned to Test             │
         └────────────────┬───────────────────────┘
                          ↓
         ┌────────────────────────────────────────┐
         │  Assertions in Test Method             │
         │  Assert.assertEquals(200, statusCode)  │
         │  Assert.assertTrue(response.body...)   │
         └────────────────┬───────────────────────┘
                          ↓
                   ┌─────────────┐
                   │  TEST PASS  │
                   └─────────────┘
```

---

## 🎬 Scenario 2: Video Answer Submission Flow (Complex)

```
┌─────────────────────────────────────────────────────────────────┐
│                    START VIDEO TEST                             │
└────────────────────────────┬────────────────────────────────────┘
                             ↓
         ┌────────────────────────────────────────┐
         │  videoService.submitVideoAnswer(       │
         │    questionId: "V123",                 │
         │    experience: "5.0",                  │
         │    filePath: "video.mp4"               │
         │  )                                     │
         └────────────────┬───────────────────────┘
                          ↓
         ┌────────────────────────────────────────┐
         │  STEP 1: Generate Upload URL           │
         │  ┌──────────────────────────────────┐  │
         │  │ generateUploadUrl(               │  │
         │  │   FileGeneratorPayload.forVideo()│  │ ← FACTORY METHOD
         │  │ )                                │  │
         │  └──────────────────────────────────┘  │
         └────────────────┬───────────────────────┘
                          ↓
         ┌────────────────────────────────────────┐
         │  POST /generate-upload-url             │
         │  Body: {                               │
         │    originalFilename: "video.webm",     │
         │    fileType: "video",                  │
         │    mimeType: "video/webm"              │
         │  }                                     │
         └────────────────┬───────────────────────┘
                          ↓
         ┌────────────────────────────────────────┐
         │  Response:                             │
         │  {                                     │
         │    fileId: "file_abc123",              │
         │    azureUpload: {                      │
         │      uploadUrl: "https://azure..."     │
         │    }                                   │
         │  }                                     │
         └────────────────┬───────────────────────┘
                          ↓
         ┌────────────────────────────────────────┐
         │  STEP 2: Submit Video Answer           │
         │  buildVideoAnswerPayload()             │
         │  ┌──────────────────────────────────┐  │
         │  │ VideoAnswerPayload.builder()     │  │
         │  │   .questionId("V123")            │  │
         │  │   .answerVideoFileId(fileId)     │  │
         │  │   .copyPasteAnalysis(            │  │
         │  │     CopyPasteAnalysis            │  │ ← SHARED
         │  │       .createDefault(53958)      │  │
         │  │   )                              │  │
         │  │   .build()                       │  │
         │  └──────────────────────────────────┘  │
         └────────────────┬───────────────────────┘
                          ↓
         ┌────────────────────────────────────────┐
         │  PATCH /update-candidate-result        │
         │  (Video answer metadata submitted)     │
         └────────────────┬───────────────────────┘
                          ↓
         ┌────────────────────────────────────────┐
         │  STEP 3: Upload Video File to Azure    │
         │  uploadFileToAzure(                    │
         │    azureUploadUrl,                     │
         │    "video.mp4"                         │
         │  )                                     │
         └────────────────┬───────────────────────┘
                          ↓
         ┌────────────────────────────────────────┐
         │  Read video file as byte array         │
         │  Set header: x-ms-blob-type: BlockBlob │
         │  PUT to Azure Blob Storage             │
         └────────────────┬───────────────────────┘
                          ↓
         ┌────────────────────────────────────────┐
         │  STEP 4: Analyze Video Response        │
         │  analyzeVideoResponse(                 │
         │    fileId: "file_abc123",              │
         │    question: "...",                    │
         │    questionId: "V123"                  │
         │  )                                     │
         └────────────────┬───────────────────────┘
                          ↓
         ┌────────────────────────────────────────┐
         │  POST /analyze-response-uri            │
         │  Body: {                               │
         │    fileId: "file_abc123",              │
         │    question: "...",                    │
         │    questionId: "V123",                 │
         │    copyPasteAnalysis: {...}            │
         │  }                                     │
         └────────────────┬───────────────────────┘
                          ↓
         ┌────────────────────────────────────────┐
         │  All responses returned to test        │
         │  Assertions performed                  │
         └────────────────┬───────────────────────┘
                          ↓
                   ┌─────────────┐
                   │  TEST PASS  │
                   └─────────────┘
```

---

## 🔄 Scenario 3: Class Interaction Diagram

```
┌──────────────────────────────────────────────────────────────────────────┐
│                         TEST EXECUTION FLOW                              │
└──────────────────────────────────────────────────────────────────────────┘

TEST CLASS                SERVICE CLASS           BASE LAYER         API
┌────────────┐           ┌─────────────┐      ┌──────────────┐   ┌──────┐
│  @Test     │           │  MCQService │      │  BaseService │   │Server│
│  method    │           │             │      │              │   │      │
└─────┬──────┘           └──────┬──────┘      └───────┬──────┘   └───┬──┘
      │                         │                     │              │
      │ 1. Call service         │                     │              │
      │ submitMCQAnswer(...)    │                     │              │
      ├────────────────────────>│                     │              │
      │                         │                     │              │
      │                    2. Build payload           │              │
      │                    MCQRootPayload            │              │
      │                         │                     │              │
      │                    3. Call inherited          │              │
      │                    updateCandidateResult()    │              │
      │                         ├────────────────────>│              │
      │                         │                     │              │
      │                         │                4. Prepare request  │
      │                         │                setHeaders()        │
      │                         │                setBody()          │
      │                         │                     │              │
      │                         │                5. Execute API     │
      │                         │                executePatchAPI()   │
      │                         │                     ├─────────────>│
      │                         │                     │              │
      │                         │                     │         6. Process
      │                         │                     │              │
      │                         │                     │  7. Response │
      │                         │                     │<─────────────┤
      │                         │                     │              │
      │                         │         8. Return Response         │
      │                         │<────────────────────┤              │
      │                         │                     │              │
      │     9. Return Response  │                     │              │
      │<────────────────────────┤                     │              │
      │                         │                     │              │
      │ 10. Assert results      │                     │              │
      │                         │                     │              │
      ↓                         ↓                     ↓              ↓
```

---

## 📊 Entity Relationship Diagram

```
┌────────────────────────────────────────────────────────────────────┐
│                    ENTITY RELATIONSHIPS                            │
└────────────────────────────────────────────────────────────────────┘

                        ┌─────────────────────┐
                        │  BaseAnswerPayload  │
                        │  (Abstract Base)    │
                        │  ─────────────────  │
                        │  - questionId       │
                        │  - type             │
                        │  - skill            │
                        │  - jobRoleId        │
                        │  - experience       │
                        └──────────┬──────────┘
                                   │ extends
          ┌────────────────────────┼────────────────────────┐
          │                        │                        │
┌─────────▼────────┐    ┌─────────▼────────┐    ┌─────────▼────────┐
│ MCQRootPayload   │    │ VideoAnswerPayload│    │AudioAnswerPayload│
│ ───────────────  │    │ ────────────────  │    │ ───────────────  │
│ + candidateAnswer│    │ + answerVideoFileId│   │ + answerAudioFileId│
│ + copyPasteAnalysis│  │ + copyPasteAnalysis│   │ + copyPasteAnalysis│
└──────────┬───────┘    └──────────┬────────┘    └──────────┬───────┘
           │                       │                        │
           │ uses                  │ uses                   │ uses
           │                       │                        │
           └───────────────────────┼────────────────────────┘
                                   │
                        ┌──────────▼──────────┐
                        │ CopyPasteAnalysis   │  ← SHARED
                        │ (entity/common)     │
                        │ ──────────────────  │
                        │ - totalDuration     │
                        │ - riskScore         │
                        │ - copyBreakdown ────┼───┐
                        └─────────────────────┘   │
                                                  │ has-a
                                   ┌──────────────▼──────────┐
                                   │   CopyBreakdown         │  ← SHARED
                                   │   (entity/common)       │
                                   │   ──────────────────    │
                                   │   - questionCopies ─────┼───┐
                                   │   - optionCopies ───────┼───┼─┐
                                   │   - fullQuestionCopies ─┼───┼─┼─┐
                                   └─────────────────────────┘   │ │ │
                                                                 │ │ │
                    ┌────────────────────────────────────────────┘ │ │
                    │  ┌───────────────────────────────────────────┘ │
                    │  │  ┌──────────────────────────────────────────┘
                    │  │  │
         ┌──────────▼──▼──▼───────┐
         │  QuestionCopies        │  ← SHARED
         │  OptionCopies          │  ← SHARED
         │  FullQuestionCopies    │  ← SHARED
         │  (entity/common)       │
         │  ─────────────────     │
         │  - count               │
         │  - averageLength       │
         │  - timestamps          │
         └────────────────────────┘


                    ┌─────────────────────────┐
                    │   TypingAnalysis        │  ← SHARED
                    │   (entity/common)       │
                    │   ──────────────────    │
                    │   - totalDuration       │
                    │   - pasteAnalysis ──────┼───┐
                    │   - focusAnalysis ──────┼───┼─┐
                    │   - qualityAnalysis ────┼───┼─┼─┐
                    └───────┬─────────────────┘   │ │ │
                            │ used by             │ │ │
          ┌─────────────────┼──────────────┐      │ │ │
          │                 │              │      │ │ │
┌─────────▼────────┐   ┌────▼────────────┐│      │ │ │
│ProgrammingPayload│   │SubjectivePayload││      │ │ │
│ + typingAnalysis │   │ + typingAnalysis││      │ │ │
└──────────────────┘   └─────────────────┘│      │ │ │
                                          │      │ │ │
         ┌────────────────────────────────┘      │ │ │
         │  ┌──────────────────────────────────┐ │ │ │
         │  │  ┌───────────────────────────────┼─┘ │ │
         │  │  │  ┌────────────────────────────┼───┘ │
         │  │  │  │  ┌─────────────────────────┼─────┘
         │  │  │  │  │
┌────────▼──▼──▼──▼──▼─────────┐
│ PasteAnalysis               │  ← SHARED
│ FocusAnalysis               │  ← SHARED
│ QualityAnalysis             │  ← SHARED
│ GlobalEventAnalysis         │  ← SHARED
│ CopyPasteCorrelations       │  ← SHARED
│ (all in entity/common)      │
└─────────────────────────────┘
```

---

## 🔧 Service Layer Pattern Visualization

```
┌──────────────────────────────────────────────────────────────────┐
│               SERVICE LAYER ARCHITECTURE                         │
└──────────────────────────────────────────────────────────────────┘

                    ┌─────────────────────┐
                    │  APIControlActions  │
                    │  ─────────────────  │
                    │  Low-level HTTP     │
                    │  RestAssured wrapper│
                    │  ─────────────────  │
                    │  + setHeader()      │
                    │  + setBody()        │
                    │  + executeGetAPI()  │
                    │  + executePostAPI() │
                    │  + executePatchAPI()│
                    └──────────┬──────────┘
                               │ extends
                    ┌──────────▼──────────┐
                    │    BaseService      │
                    │    ────────────     │
                    │    Common business  │
                    │    logic layer      │
                    │    ────────────     │
                    │ + setJsonHeaders()  │
                    │ + generateUploadUrl()│
                    │ + uploadFileToAzure()│
                    │ + updateCandidate() │
                    │ + analyzeResponse() │
                    └──────────┬──────────┘
                               │ extends
         ┌─────────────────────┼─────────────────────┐
         │                     │                     │
┌────────▼────────┐  ┌─────────▼────────┐  ┌────────▼────────┐
│  MCQService     │  │  VideoServices   │  │  AudioService   │
│  ─────────────  │  │  ──────────────  │  │  ─────────────  │
│  MCQ-specific   │  │  Video-specific  │  │  Audio-specific │
│  operations     │  │  operations      │  │  operations     │
│  ─────────────  │  │  ──────────────  │  │  ─────────────  │
│ + submitMCQ()   │  │ + submitVideo()  │  │ + submitAudio() │
└─────────────────┘  └──────────────────┘  └─────────────────┘

BENEFIT: Each service focuses on ONE responsibility
```

---

## 💡 Shared Components Usage Map

```
┌────────────────────────────────────────────────────────────────────┐
│          HOW SHARED COMPONENTS ARE REUSED                          │
└────────────────────────────────────────────────────────────────────┘

entity/common/CopyPasteAnalysis
    │
    ├─── Used by MCQRootPayload
    ├─── Used by AudioAnswerPayload
    ├─── Used by AudioSubmitAnswerPayload
    ├─── Used by VideoAnswerPayload
    └─── Used by VideoAnswerAnalysisPayload

entity/common/TypingAnalysis
    │
    ├─── Used by ProgrammingPayload
    └─── Used by SubjectiveRootPayload

entity/common/FileGeneratorPayload
    │
    ├─── forAudio() → Used by AudioService
    └─── forVideo() → Used by VideoServices

entity/common/QuestionCopies
entity/common/OptionCopies
entity/common/FullQuestionCopies
    │
    └─── Used by CopyBreakdown (which is used by CopyPasteAnalysis)

entity/common/PasteAnalysis
entity/common/FocusAnalysis
entity/common/QualityAnalysis
entity/common/GlobalEventAnalysis
entity/common/CopyPasteCorrelations
    │
    └─── Used by TypingAnalysis

RESULT: Write once, use everywhere!
        No code duplication
        Single source of truth
```

---

## 📈 Request/Response Flow Sequence

```
┌────────────────────────────────────────────────────────────────────┐
│                  API REQUEST LIFECYCLE                             │
└────────────────────────────────────────────────────────────────────┘

1. TEST METHOD
   │
   ├─ Creates service instance
   └─ Calls service method with parameters
      │
      ↓
2. SERVICE LAYER
   │
   ├─ Builds POJO using Lombok builder
   ├─ Uses shared entities (CopyPasteAnalysis, etc.)
   ├─ Converts POJO to JSON
   └─ Calls BaseService method
      │
      ↓
3. BASE SERVICE
   │
   ├─ Sets common headers (JSON)
   ├─ Sets request body
   └─ Calls APIControlActions method
      │
      ↓
4. API CONTROL ACTIONS
   │
   ├─ Builds RequestSpecBuilder
   ├─ Adds all headers
   ├─ Adds body
   ├─ Sets base URI from config
   └─ Executes HTTP method (GET/POST/PATCH/PUT)
      │
      ↓
5. REST ASSURED
   │
   ├─ Serializes request
   ├─ Sends HTTP request over network
   └─ Waits for response
      │
      ↓
6. API SERVER
   │
   ├─ Processes request
   ├─ Executes business logic
   └─ Returns response
      │
      ↓
7. REST ASSURED
   │
   ├─ Receives HTTP response
   ├─ Parses response
   └─ Returns Response object
      │
      ↓
8. API CONTROL ACTIONS
   │
   └─ Returns Response to caller
      │
      ↓
9. BASE SERVICE
   │
   └─ Returns Response to caller
      │
      ↓
10. SERVICE LAYER
    │
    └─ Returns Response to test
       │
       ↓
11. TEST METHOD
    │
    ├─ Extracts status code
    ├─ Extracts response body
    ├─ Performs assertions
    └─ Test PASS/FAIL
```

---

## 🎯 Quick Reference Guide

### When to Use What

| Task | Use This Class | Example |
|------|---------------|---------|
| Submit MCQ answer | `MCQService` | `mcqService.submitMCQAnswer(...)` |
| Submit video | `VideoServices` | `videoService.submitVideoAnswer(...)` |
| Submit audio | `AudioService` | `audioService.submitAudioAnswer(...)` |
| Get screening details | `ScreeningDetailsServices` | `service.getScreeningDetails(id)` |
| Create copy-paste data | `CopyPasteAnalysis` | `CopyPasteAnalysis.createDefault(duration)` |
| Create typing data | `TypingAnalysis` | `TypingAnalysis.createDefault(...)` |
| Generate file upload | `FileGeneratorPayload` | `FileGeneratorPayload.forVideo()` |

### Common Patterns

```java
// Pattern 1: Simple API call
Service service = new Service();
Response response = service.methodName(params);
Assert.assertEquals(200, response.statusCode());

// Pattern 2: With shared entity
Payload payload = Payload.builder()
    .field1(value)
    .sharedField(SharedEntity.createDefault())
    .build();

// Pattern 3: File upload
Response urlResponse = generateUploadUrl(FileGeneratorPayload.forVideo());
String fileId = extractFileId(urlResponse);
uploadFileToAzure(uploadUrl, filePath);
```

---

**Document Created**: December 30, 2025  
**Purpose**: Visual guide for framework understanding  
**Audience**: QA Engineers, Developers, New Team Members

